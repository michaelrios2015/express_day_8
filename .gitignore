# note vscode starts as utf-16 you need to convert it to utf-8 
# what is the difference.... not a clue unicode transmistion format
# 8 bit as opposed to 16 bit which git does not like ...interesting
node_modules